import 'jest';

describe('dummy', () => {
    beforeEach(() => {
        console.log('Before each');
    });

    it('should work as expected', async () => {
        console.log('it should work!');
    });
});